{
  "name": "workflow-orchestration",
  "version": "1.0.0",
  "description": "AI-powered workflow orchestration platform with Go backend",
  "private": true,
  "workspaces": [
    "packages/frontend",
    "packages/shared"
  ],
  "scripts": {
    "dev": "concurrently \"cd backend && make dev\" \"npm run dev -w packages/frontend\"",
    "build": "npm run build -w packages/shared && cd backend && make build && cd .. && npm run build -w packages/frontend",
    "backend": "cd backend && make dev",
    "frontend": "npm run dev -w packages/frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
